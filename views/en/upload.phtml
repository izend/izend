<?php

/**
 *
 * @copyright  2012-2014 izend.org
 * @version    9
 * @link       http://www.izend.org
 */
?>
<form enctype="multipart/form-data" action="" method="post" class="compact">
<input type="hidden" name="upload_token" value="<?php echo $token; ?>" />
<input type="hidden" name="MAX_FILE_SIZE" value="<?php echo $maxfilesize; ?>" />
<p>
<input type="file" name="upload_file" id="upload_file" title="" />
<input type="submit" class="submit submit_put" id="upload_put" name="upload_put" value="Upload" title="" />
<?php extract($errors); ?>
<?php if ($missing_file or $bad_file or $bad_name or $bad_size or $bad_file or $bad_copy or $copy_error): ?>
<span class="icon icon_ko">-</span>
<?php else: ?>
<?php extract($infos); ?>
<?php if ($file_copied): ?>
<span class="icon icon_ok">+</span>
<?php endif; ?>
<?php endif; ?>
</p>
</form>
<script type="text/javascript">
//<![CDATA[
$(document).ready(function() {
    var file=$('#upload_file');
    var upload=$('#upload_put');

    $('#upload_file, #upload_put').wrapAll('<span style="position:relative;overflow:hidden;cursor:pointer"></span>');
    file.css({height: upload.outerHeight(true) + 'px', fontSize: '100px', position: 'absolute', right: 0, opacity: 0, cursor: 'pointer'});

    file.change(function() {
        if (file.val()) {
            upload.click();
        }
    });
});
//]]>
</script>
